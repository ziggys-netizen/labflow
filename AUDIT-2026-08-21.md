# LabFlow code audit — 21 August 2026

Scope: working tree on disk at `C:\Users\kanui\Documents\LF1` (committed HEAD **plus** uncommitted files). No other files were written. `docs/PRD.md` is context only.

**Git snapshot:** branch work is on `main`. HEAD `acecb8f`. Uncommitted **modified:** `app/dashboard/page.tsx`, `app/lib/AppNav.tsx`, `app/lib/AuthContext.tsx`, `app/lib/clinicScope.ts`, `app/lib/permissions.ts`, `app/login/page.tsx`, `app/owner/page.tsx`, `app/staff/page.tsx`, `package.json`, `package-lock.json`. Uncommitted **untracked:** `app/inventory/` (four pages), `app/lib/identity.ts`, `app/lib/inventory.ts`, `app/lib/membership.ts`, `app/lib/migration.ts`, `app/owner/clinics/[clinicId]/migration/page.tsx`, `app/profile/page.tsx`. Those untracked paths exist on disk and are included below; they are **not in HEAD**.

---

### 1. Stack, as built

- **Framework:** Next.js `16.3.0` App Router (`package.json`). TypeScript (`tsconfig.json`: `strict: true`, path alias `@/*` → `./*`). React `19.2.8`.
- **Styling:** Tailwind CSS v4 via `@tailwindcss/postcss` (`postcss.config.mjs`, `app/globals.css`). Geist fonts in `app/layout.tsx`.
- **Data:** Firebase Firestore client SDK (`firebase` `^12.17.1`). All clinical reads/writes happen in browser `"use client"` pages. **No** `app/**/route.ts`, **no** `middleware.ts`, **no** Server Actions.
- **Auth:** Firebase Auth Google popup (`signInWithPopup` in `app/lib/AuthContext.tsx`). Config from `NEXT_PUBLIC_FIREBASE_*` in `app/lib/firebase.ts`.
- **Hosting:** README still says Vercel. **No** `vercel.json` in the repo. Deployed project is not verified from this tree.
- **Spreadsheets:** `@e965/xlsx` `^0.20.3` is in the **working-tree** `package.json` only (not in HEAD). Used to **parse** imports, not export.
- **Lint/build scripts:** `next dev|build|start`, `eslint`. No test runner, no `firebase` CLI config.
- **Layout/metadata:** root `app/layout.tsx` wraps the app in `AuthProvider`. Title/description still `"Create Next App"` / `"Generated by create next app"`.
- **Missing directories:** no `components/`, `hooks/`, `utils/`, `types/`, or top-level `lib/`. Helpers live under `app/lib/`.
- **Firebase project files:** `firestore.indexes.json` exists (composite indexes for `patients`, `orders`, `testCatalog`). **`firebase.json` is missing. `firestore.rules` is missing.**

---

### 2. Route map

Every route is a client page unless noted. Permission is **client-only**. `ProtectedRoute` only gates login / pending / rejected / owner-bypass; it does **not** check capability.

| Path | File | What it renders | Roles that get a useful UI | How permission is enforced |
| --- | --- | --- | --- | --- |
| `/` | `app/page.tsx` | Public marketing homepage + “Register a patient” CTA | Anyone (no auth) | None. **Not** wrapped in `ProtectedRoute`. |
| `/login` | `app/login/page.tsx` | Google sign-in; if already signed in, links to Patients (and Owner if `role === "owner"`) | Unauthenticated; signed-in users see a bounce screen | None. Not `ProtectedRoute`. |
| `/join` | `app/join/page.tsx` | Join-code form | `pending` with no `clinicId` | Client `ProtectedRoute` + page logic. Writes `users.clinicId` only. |
| `/pending` | `app/pending/page.tsx` | Awaiting approval / access declined | `pending` with `clinicId`, or `rejected` | Client `ProtectedRoute`. |
| `/profile` | `app/profile/page.tsx` | Username + clinic memberships (**untracked**) | Any signed-in non-blocked user | Client `ProtectedRoute` only. |
| `/register` | `app/register/page.tsx` | Patient registration form | Any signed-in user **with a `clinicId`** | Client `ProtectedRoute`. Extra clinicId check; owner blocked (see §4). **No role gate.** |
| `/patients` | `app/patients/page.tsx` | Patient table, delete, sample checkbox, print link | Any signed-in user who can load the page | Client `ProtectedRoute`. Nav hidden for storekeeper; **URL still works.** Delete has no role check. Sample checkbox uses `canRecordSampleCollection`. |
| `/patients/[patientId]/print` | `app/patients/[patientId]/print/page.tsx` | A4 print sheet, auto `window.print` | Same as patients | Client `ProtectedRoute` + post-read `clinicId` match (owner skipped). |
| `/orders` | `app/orders/page.tsx` | Order list | Same as patients | Client `ProtectedRoute`. `getClinicDocs("orders")`. |
| `/orders/new/[patientId]` | `app/orders/new/[patientId]/page.tsx` | Test search + create order | Signed-in with `clinicId` | Client `ProtectedRoute`. Owner blocked (quoted in §4). **No role gate.** |
| `/orders/[orderId]` | `app/orders/[orderId]/page.tsx` | Results entry, sample collection, review UI | Anyone who can open the order | Client `ProtectedRoute`. Sample: `canRecordSampleCollection`. Review: `role === "admin"` (dead). Results submit: **status only, no role.** |
| `/dashboard` | `app/dashboard/page.tsx` | Queue + TAT metrics | `owner`, `clinic_admin`, `lab_manager` | Client `ProtectedRoute` + `canViewDashboard`; others `router.replace(landingPathForRole)`. |
| `/settings` | `app/settings/page.tsx` | Edit/add test catalogue | Intended `admin` only | **Not** `ProtectedRoute`. Redirects unless `role === "admin"`. Unreachable with live roles. |
| `/staff` | `app/staff/page.tsx` | Pending approvals, staff grouped by clinic, role assign | `owner`, `clinic_admin` | Client `ProtectedRoute` + `canManageStaff` (working tree) / inline check. |
| `/owner` | `app/owner/page.tsx` | Create clinic, list clinics + join codes, assign first clinic admin | `owner` | Client `ProtectedRoute` + `role === "owner"`. |
| `/owner/clinics/[clinicId]/migration` | `app/owner/clinics/[clinicId]/migration/page.tsx` | Spreadsheet import + assign-unassigned-records (**untracked**) | `owner` | Client `ProtectedRoute` + `role === "owner"`. |
| `/inventory` | `app/inventory/page.tsx` | Store dashboard (**untracked**) | All five live roles via `canViewInventory` | Client `ProtectedRoute` + `canViewInventory`; others redirect `/patients`. |
| `/inventory/items` | `app/inventory/items/page.tsx` | Item master (**untracked**) | View: `canViewInventory`. Edit: owner / lab_manager / storekeeper | Same pattern. |
| `/inventory/movements` | `app/inventory/movements/page.tsx` | Stock in/out (**untracked**) | Record: owner / lab_manager / storekeeper | Same pattern. |
| `/inventory/specimens` | `app/inventory/specimens/page.tsx` | Specimen custody log (**untracked**) | Record: owner / lab_manager / technician / storekeeper; **owner write is then blocked** for lack of clinic | Same pattern. |

`AppNav` (`app/lib/AppNav.tsx`): Patients/Orders if not storekeeper; Store if `canViewInventory`; Dashboard if `canViewDashboard`; Clinic Settings if `role === "admin"` (dead); Manage Staff if owner or clinic_admin; Owner if owner. Clinic switcher if `memberships.length > 1` (**uncommitted**).

---

### 3. Firestore data model, as actually written

No subcollections. All top-level. Types below are what the writers put on the wire (ISO date strings, not Firestore Timestamps).

#### `users/{uid}`

Written by `app/lib/AuthContext.tsx` (create), `app/join/page.tsx`, `app/owner/page.tsx`, `app/staff/page.tsx`, `app/lib/identity.ts` (`claimUsername`), `app/lib/AuthContext.tsx` (`setActiveClinic` / `legacyMirror`), migration assign (`users/{ownerUid}`).

Create (AuthContext ~89–101): `email`, `name`, `role: "pending"`, `clinicId: null`, `status: "pending"`, `username: null`, `clinicRoles: {}`, `activeClinicId: null`, `createdAt`, `approvedBy: null`, `approvedAt: null`.

Join (~35–37): **only** `clinicId` (clinic document id). Does not write `clinicRoles`, `clinicIds`, or `status`.

Staff/owner membership writes: `clinicRoles.{clinicId}` object `{ role, status, createdAt, approvedByUid, approvedByUsername, approvedByEmail, approvedAt }`, `clinicIds: string[]`, and when made active: `role`, `clinicId`, `status`, `activeClinicId`, `approvedBy`, `approvedByUid`, `approvedByUsername`, `approvedAt`.

Username: `username`, `usernameUpdatedAt`.

**Read:** `resolveIdentity` (`app/lib/membership.ts`, untracked) prefers `role === "owner"` and then `clinicRoles` + legacy `role`/`clinicId`/`status`/`activeClinicId`.

#### `usernames/{username}` (untracked writer)

`app/lib/identity.ts` transaction: `{ uid, username, updatedAt }`. Deleted when the same user changes name.

#### `clinics/{id}`

Owner create (`app/owner/page.tsx` ~118–129): `name`, `address`, `tin`, `businessRegNumber`, `responsiblePerson`, `joinCode` (7-char A–Z0–9), `createdAt`, `createdBy` (uid), `active: true`, `brandColor: null`.

**No edit/update path** after create. Join reads `joinCode` and `active`.

#### `patients/{id}`

Register (`app/register/page.tsx` ~316–331): `labId`, `name`, `preferredName`, `sex`, `dob`, `phone`, `address`, `nationalId`, `nextOfKin`, `referringClinician`, `reasonForVisit`, `consentGiven: true`, `createdAt`, `clinicId`.

Hard `deleteDoc` from `app/patients/page.tsx` ~194. No tombstone.

Migration import can also write `sampleCollectedAt` / `sampleCollectedBy` **onto the patient document** (`app/lib/migration.ts` PatientImportData). Runtime never reads those fields from `patients`.

#### `orders/{id}`

Create (`app/orders/new/[patientId]/page.tsx` ~136–144): `patientId`, `patientName`, `patientLabId`, `tests: [{code,name}]`, `status: "pending"`, `createdAt`, `clinicId`.

Sample collection (order page + patient checkbox): `sampleCollectedAt`, `sampleCollectedBy`, `sampleCollectedSource` (`"order"` | `"patient_checkbox"`), `sampleCollectionQuickAction: null`, sometimes `clinicId`.

Results submit: `results` (map testCode → paramName → string), `status: "results_entered"`, `resultsEnteredBy` (email), `resultsEnteredAt`.

Approve: `status: "approved"`, `reviewedBy` (email), `reviewedAt`, `reviewNotes: ""`.

Send back: `status: "needs_correction"`, `reviewedBy`, `reviewedAt`, `reviewNotes`.

#### `testCatalog/{id}`

IDs: `${clinicId}_${code}` when seeded/imported. Fields: `code`, `name`, `category`, `parameters: [{name,unit,referenceRange}]`, `price`, `clinicId`. Written by `app/settings/page.tsx` (unreachable) and migration import.

In-memory fallback: 16 tests in `app/lib/testCatalog.ts` (`TEST_CATALOG`) used when Firestore query is empty.

#### Inventory (untracked)

`inventoryItems`: `clinicId`, `name`, `category`, `testCode`, `manufacturer`, `supplier`, `catalogueCode`, `packingUnit`, `unitsPerPack`, `baseUnit`, `unitSize`, `packsPerCarton`, `storageCondition`, `department`, `minimumStock`, `active`, `updatedAt`, `createdAt`, `createdBy` (ActorStamp).

`inventoryBatches`: `clinicId`, `itemId`, `itemName`, `lotNumber`, `expiryDate`, `manufactureDate`, `supplier`, `location`, `acceptance` (`accepted`|`untested`|`rejected`), `createdAt`, `createdBy`.

`inventoryMovements`: `clinicId`, `itemId`, `itemName`, `batchId`, `lotNumber`, `expiryDate`, `type` (`receipt`|`issue`|`transfer`|`return`|`adjustment`|`disposal`), `direction` (`in`|`out`|`none`), `quantity`, `packingUnit`, `unitsPerPack`, `baseUnit`, `occurredAt`, `recordedAt`, `actor`, `supplier`, `deliveryNote`, `conditionOnArrival`, `department`, `issuedTo`, `purpose`, `destination`, `reason`, `note`. **Balances are not stored.**

`specimenMovements`: `clinicId`, `direction` (`received`|`sent`), `specimenType`, `container`, `quantity`, `orderReference`, `department`, `destination`, `occurredAt`, `recordedAt`, `actor`, `condition`, `transport`, `status` (`in_lab`|`in_transit`|`delivered`|`rejected`|`returned`), `note`.

No `reagents` / `stock` collection names. “Reagent” is an `inventoryItems.category`.

#### `migrationHistory/{id}` (untracked)

Aggregate import reports: `clinicId`, `clinicName`, `dataType`, `fileName`, counts, `status` (`completed`|`partial_failure`|`failed`), `createdAt`, `createdByEmail`, `createdByUid`, optional `collectionCounts`.

#### Call-outs requested

| Topic | Finding |
| --- | --- |
| `sampleCollectedAt` / `sampleCollectedBy` | **Written and read on `orders`.** Written on `patients` only by spreadsheet import; **never read** from patients. Sources: order page, patient checkbox, historical-order import. |
| Soft-delete flags | **None.** Patient delete is `deleteDoc`. Inventory items use `active` boolean (retire/restore), not a recycle bin. |
| Audit log / `updatedBy` / `approvedBy` / `approvedAt` | **No `auditLogs` collection. No `updatedBy` field.** Order review uses `reviewedBy`/`reviewedAt`/`reviewNotes` (emails). Staff uses `approvedBy*` on the user/membership. Inventory uses `createdBy` / `actor` stamps (working tree). `usernameUpdatedAt` on users. |
| `preApprovals` | **No collection.** Migration Center lists “Staff pre-approvals” as unavailable (`app/owner/clinics/[clinicId]/migration/page.tsx` ~143–147). |
| Inventory / reagent / stock | Working-tree collections above. Not in HEAD. |

**Written, never meaningfully read in UI:** `clinics.brandColor`, `clinics.createdBy`, `users.usernameUpdatedAt`, `patients.consentGiven` (enforced at register, not displayed later), patient-level `sampleCollectedAt`/`sampleCollectedBy` from import, `migrationHistory.createdByUid` (UI shows email).

**Read, maybe never written by current UI:** `sampleCollectionQuickAction.source === "patients_table"` (legacy unpublished shape; current writers set the field to `null`). `users.clinicRoles` is empty until staff/owner approval (join does not write it; `resolveIdentity` synthesizes from legacy fields).

---

### 4. Roles and permissions, as enforced

#### Role string literals in code

Live / stored: `"owner"`, `"clinic_admin"`, `"lab_manager"`, `"technician"`, `"storekeeper"`, `"pending"`.

Dead but still tested: `"admin"` (`AppNav.tsx:73`, `orders/[orderId]/page.tsx:203`, `settings/page.tsx:36,69,212`).

Also reserved **usernames** (not roles) in `app/lib/identity.ts`: `admin`, `administrator`, `clinic_admin`, `clinicadmin`, `lab_manager`, `labmanager`, `owner`, `storekeeper`, `stockkeeper`, `technician`, plus help/system names.

User **status** literals (not roles): `"pending"`, `"approved"`, `"rejected"`.

#### Canonical list vs staff dropdown

Defined: `app/lib/permissions.ts` lines 7–14 (`ROLES`) and 19–24 (`ASSIGNABLE_ROLES`). `owner` is not assignable.

Staff `<select>` populated in `app/staff/page.tsx` `RoleSelect`, lines 82–86, mapping `ASSIGNABLE_ROLES`. Default draft often `"technician"` (lines 223, 598, 702).

(HEAD staff page had a local `ASSIGNABLE_ROLES` constant; working tree moved it to `permissions.ts`.)

#### What each role can actually do (guard conditions on disk)

| Role | Can do in the UI | Blocked / broken |
| --- | --- | --- |
| `owner` | Pass `ProtectedRoute` always. See all clinics’ patients/orders/inventory via unfiltered `getClinicDocs`. Create clinics, assign clinic admin, open Migration Center, manage all staff, view dashboard, record sample collection **if they open an order**, enter results **if they open an order**, manage inventory items/movements **if they pick a clinic on the item**. | **Cannot register a patient or create an order** (no `clinicId`). **Cannot approve results** (`admin` gate). Specimen log write blocked (no clinic). Nav Settings dead. |
| `clinic_admin` | Staff page, join code display, dashboard, patients/orders/register/results-entry (no extra role gate), view inventory. | **Cannot** record sample collection. **Cannot** approve results. **Cannot** edit test catalogue (settings is `admin`). **Cannot** edit clinic profile (no page). **Cannot** record stock movements. |
| `lab_manager` | Dashboard, patients/orders/register, sample collection, results entry, view+record inventory and specimens. | **Cannot** approve results (the control is `admin`). **Cannot** manage staff. |
| `technician` | Patients/orders/register, sample collection, results entry, view inventory, record specimens. | No dashboard, no stock in/out, no staff, no approve. |
| `storekeeper` | Inventory landing (`landingPathForRole` → `/inventory`), view stock, record stock, record specimens, manage items. Clinical nav hidden. | **No server/route block** on `/patients`, `/register`, `/orders`, `/orders/[id]` — URL access works; they can enter results. No dashboard. |
| `pending` / `rejected` | Join or pending screens. | `ProtectedRoute` redirects everything else to `/join` or `/pending`. |
| `admin` | Would unlock Settings + result approval. | **Not a stored role.** Unreachable. |

#### Owner: create patient, create order, enter results?

- **Create patient:** no, because `clinicId` is forced null for owners. `app/register/page.tsx` 299–305.
- **Create order:** no, same reason. Exact error:

```130:132:app/orders/new/[patientId]/page.tsx
    if (!clinicId && isOwner(role)) {
      setStatus("Owner accounts are not assigned to a clinic. Use a clinic staff account to create orders.");
      return;
```

Register uses the same sentence with “register patients” (`app/register/page.tsx:304`).

- **Enter results:** **yes**, if they open `/orders/[orderId]`. `submitForReview` has no role check (`app/orders/[orderId]/page.tsx` 124–135). Owner lists every order via `getClinicDocs`.

`resolveIdentity` documents why owner has no clinic (`app/lib/membership.ts` 68–71, 82–91). `setActiveClinic` no-ops for owner (`AuthContext.tsx:133`).

#### Who can approve/release a result?

Guard:

```203:203:app/orders/[orderId]/page.tsx
  const canReview = role === "admin";
```

UI:

```358:358:app/orders/[orderId]/page.tsx
        {canReview && order.status === "results_entered" && (
```

Functions `approveAndRelease` / `sendBackForCorrection` exist (145–184) but **no live role** is `"admin"`. So for `results_entered`, the page **shows status and submit-again**, and **does not show Approve**. That is a **dead gate**, not “unimplemented functions” and not an explicit `lab_manager` check.

**`lab_manager` cannot approve. `owner` cannot approve. `clinic_admin` cannot approve.**

#### Can `clinic_admin` approve results?

**No.** Condition: `canReview` requires `"admin"`. There is no Firestore rule either, so a clinic_admin **client** cannot see the button; a raw SDK write is not prevented (not verified against the hosted project).

#### Does `ProtectedRoute` let owner through every check?

**Yes** — `getDestination` returns `null` (no redirect) when `role === "owner"`:

```7:18:app/lib/ProtectedRoute.tsx
function getDestination(
  user: { uid: string } | null,
  role: string | null,
  status: string | null,
  clinicId: string | null
): string | null {
  if (!user) return "/login";
  if (role === "owner") return null;
  if (status === "pending" && !clinicId) return "/join";
  if (status === "pending" && clinicId) return "/pending";
  if (status === "rejected") return "/pending";
  return null;
}
```

It does **not** check capabilities. Owner still hits per-page clinicId blocks on create.

---

### 5. Order and result lifecycle

**Status string literals written:** `"pending"`, `"results_entered"`, `"approved"`, `"needs_correction"`.

Display overlay (not a stored status): if `!sampleCollectedAt`, lists/print/detail show **“Awaiting sample”** even when `status === "pending"`.

| From | To | Function | Role gate | Reachable in UI? |
| --- | --- | --- | --- | --- |
| (create) | `pending` | `handleCreateOrder` | clinicId required; owner blocked | Yes |
| `pending` \| `results_entered` \| `needs_correction` | `results_entered` | `submitForReview` | **none** | Yes |
| `results_entered` | `approved` | `approveAndRelease` | `role === "admin"` | **No** (dead role) |
| `results_entered` | `needs_correction` | `sendBackForCorrection` | `role === "admin"` | **No** |
| any except display | — | sample collection fields | `canRecordSampleCollection` | Yes; independent of status |
| `approved` | (none) | inputs disabled via `resultsEditable` | — | Terminal in UI |

`resultsEditable` (`orders/[orderId]/page.tsx:86`) is status-only. After `approved`, locked.

**Unreachable via UI, reachable via import:** `approved` and `needs_correction` (and their audit fields) through historical-order migration (`app/lib/migration.ts` `HistoricalOrderStatus`, `normalizeStatus` maps `ordered`→pending, `released`→approved).

No cancelled/voided/in_progress statuses. Dashboard “Returned for correction” will stay 0 until import or the `admin` gate is fixed.

Patient-list checkbox only toggles collection when exactly one current uncollected order exists (`app/lib/sampleCollection.ts`). Uncheck only if source was `patient_checkbox`.

---

### 6. Multi-tenancy and isolation

**Attach:** create paths set `clinicId` from `useAuth().clinicId` (patients, orders) or owner-selected clinic (inventory items, migration). Owner identity forces `clinicId: null`.

**Read scope:** `clinicCollectionQuery` / `getClinicDocs` (`app/lib/clinicScope.ts` 17–31):

- Owner: **no** `where("clinicId")` — whole collection.
- Non-owner without clinicId: `where("clinicId","==","__none__")` (matches nothing).
- Else: `where("clinicId","==", clinicId)`.

Document `getDoc` by id (patient, order, clinic) then a **client** mismatch check (`!isOwner && clinicId && data.clinicId && data.clinicId !== clinicId`). Guessable IDs are not filtered by the database.

#### Queries / full-collection reads **without** a `clinicId` equality filter

1. Owner path of `clinicCollectionQuery` — `query(col)` (`clinicScope.ts:24–25`) for patients, orders, testCatalog, inventory*, specimens.
2. Owner `loadClinicNames` — `getDocs(collection(db,"clinics"))` (`clinicScope.ts:76`).
3. Join — clinics `where("joinCode","==", …)` (`join/page.tsx:28`).
4. Owner `uniqueJoinCode` — same joinCode query (`owner/page.tsx:45`).
5. Owner clinic list — `getDocs(collection(db,"clinics"))` (`owner/page.tsx:73`).
6. Owner assign admin — `users` `where("email","==", …)` (`owner/page.tsx:157–160`).
7. Staff, owner — `getDocs(collection(db,"users"))` (`staff/page.tsx:178`).
8. Staff, clinic_admin — `users` where `clinicId ==` and `clinicIds` array-contains (`staff/page.tsx:179–183`) — filters **users**, not clinical collections.
9. New-order pending list, **owner** — `orders` where `patientId` + `status==pending` **without clinicId** (`orders/new/[patientId]/page.tsx:66–67`).
10. Migration `assignExistingRecords` — `getDocs` entire `patients`, `orders`, `testCatalog` (`migration/page.tsx:772`).
11. Auth — `getDoc(users/{uid})`. Identity — `usernames/{username}`.
12. Direct `getDoc` of `patients/{id}`, `orders/{id}`, `clinics/{id}` on detail/print/join.

Staff secondary query uses `clinicIds`, not `clinicId`.

**Firestore rules:** **do not exist in this repository.** There is no `firestore.rules` and no `firebase.json`. What is deployed on the Firebase project is **not verified**. Isolation is a browser convention only. The migration page states this explicitly (~1416–1419).

---

### 7. Clinic and staff management

**Owner console (`/owner`):** create clinic (name, address, TIN, business reg, responsible person, generated join code), list clinics with join codes and IDs, assign first clinic administrator by email (must already have signed in). Working tree adds “Continue to Data Migration” / “+ Add Data” links. HEAD still had an on-page **Run migration** button; that UI was **moved** to the untracked migration page.

**Clinic list:** on `/owner` only. No public clinic directory.

**Clinic profile page:** **none.** No `/clinics/[id]` editor. `clinic_admin` cannot change name/address/TIN. Join code is shown read-only on `/staff` for clinic_admin (`staff/page.tsx:552–555`).

**Staff nav:** `AppNav` “Manage Staff” for owner and clinic_admin.

**Join-code E2E (as coded):**

1. Google popup → `users/{uid}` created `role/status: pending`, `clinicId: null`.
2. `ProtectedRoute` → `/join`.
3. Code uppercased, maxLength 7; query `clinics.joinCode`; skip `active === false`.
4. `updateDoc` **only** `{ clinicId: match.id }`.
5. Treat as pending-with-clinic → `/pending` (or the join success screen).
6. clinic_admin/owner on `/staff` loads users by `clinicId` / `clinicIds` / all users; pending section Approve/Reject with role dropdown.
7. Approval writes `clinicRoles.*` + legacy mirror `status: "approved"`.

Gap: join does not write `clinicRoles` or `clinicIds`. It still works for a single clinic because `resolveIdentity` synthesizes a membership from legacy `clinicId` + `role: "pending"`.

**“Run migration” today:** HEAD owner page stamped `clinicId` onto patients/orders/testCatalog where missing or `"default-clinic"`, then set the current user to owner. Working tree: same operation is **Assign existing records** on `/owner/clinics/[clinicId]/migration` (`assignExistingRecords`, ~758–857). It scans **all** docs in those three collections, updates those with empty/`default-clinic` clinicId to **this** clinic, then `updateDoc` the owner user `{ role:"owner", status:"approved", clinicId:null }`. It does **not** import a spreadsheet and does **not** run automatically. Spreadsheet import is a separate wizard (patients, test catalogue, historical orders).

**Staff grouped by clinic:** working-tree `/staff` builds `groups` keyed by `membership.clinicId` (`staff/page.tsx:297–310`), collapsible; owner defaults collapsed if more than one clinic (`626`). Owner accounts listed separately (“Platform owner”). **This rewrite is uncommitted.**

---

### 8. Printing and reports

**Print views:** only `/patients/[patientId]/print`. Auto-print after load (`setTimeout(..., 300)` line 146) and a Print button (line 174). Both call `window.print()`. A4 CSS in `PRINT_CSS` (lines 48–56). Only `status === "approved"` results print; otherwise “Results not released.”

**PDF:** none (no jspdf / server PDF).

**Excel/CSV export:** **none.** `@e965/xlsx` parses **uploads** in Migration Center. Dashboard footer (`dashboard/page.tsx:232–233`): “Reporting beyond the current week is by Excel export, which is not built yet — it depends on an email delivery provider being chosen.”

**Email:** no SendGrid, Resend, or nodemailer. Optional `NEXT_PUBLIC_SUPPORT_EMAIL` mailto on the migration page. Result “delivery” is print + on-screen.

#### Lab ID — generate and render (file + line + styling)

| Location | Lines | Styling |
| --- | --- | --- |
| Generate `LF-YYYYMMDD-XXXX` | `app/register/page.tsx` 173–177 | n/a |
| After register | `app/register/page.tsx` 514–517 | `text-sm text-gray-900 font-medium` |
| Patients table header / cell | `app/patients/page.tsx` 229, 269 | `text-gray-900 whitespace-nowrap` — **not** mono. Clinic ID column is `font-mono text-xs` (264). |
| Print header | `app/patients/[patientId]/print/page.tsx` 197 | `text-sm text-gray-600` `Lab ID: {patient.labId}` |
| Print field grid | same file 201 | `Field`: label `text-[10px] uppercase tracking-wide text-gray-500`, value `text-sm text-gray-900` |
| Orders list | `app/orders/page.tsx` 76 | `text-sm text-gray-500` |
| New order heading | `app/orders/new/[patientId]/page.tsx` 168 | `text-gray-600` |
| Order detail | `app/orders/[orderId]/page.tsx` 218 | `text-gray-600` |

Migration can allocate Lab IDs if the sheet omits them (`app/lib/migration.ts` ~751–755). Specimen “Order / Lab ID” column is free-text `orderReference`, not the generator.

---

### 9. Build health

#### `npx tsc --noEmit` (verbatim)

```
npm warn Unknown env config "devdir". This will stop working in the next major version of npm. See `npm help npmrc` for supported config options.
```

Exit code **0**. No TypeScript errors.

#### `npm run lint` (verbatim body)

```
> labflow@0.1.0 lint
> eslint

C:\Users\kanui\Documents\LF1\app\lib\AppNav.tsx
  49:9   error  Do not use an `<a>` element to navigate to `/`. Use `<Link />` from `next/link` instead. See: https://nextjs.org/docs/messages/no-html-link-for-pages         @next/next/no-html-link-for-pages
  58:15  error  Do not use an `<a>` element to navigate to `/orders/`. Use `<Link />` from `next/link` instead. See: https://nextjs.org/docs/messages/no-html-link-for-pages  @next/next/no-html-link-for-pages

C:\Users\kanui\Documents\LF1\app\login\page.tsx
  16:19  error  Unexpected any. Specify a different type                         @typescript-eslint/no-explicit-any
  34:48  error  `'` can be escaped with `&apos;`, `&lsquo;`, `&#39;`, `&rsquo;`  react/no-unescaped-entities

C:\Users\kanui\Documents\LF1\app\patients\page.tsx
  119:5  error    Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

C:\Users\kanui\Documents\LF1\app\patients\page.tsx:119:5
  117 |
  118 |   useEffect(() => {
> 119 |     fetchPatients();
      |     ^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  120 |   }, [role, clinicId]);
  121 |
  122 |   async function toggleSampleCollected(patient: Patient, collected: boolean) {  react-hooks/set-state-in-effect
  120:6  warning  React Hook useEffect has a missing dependency: 'fetchPatients'. Either include it or remove the dependency array                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          react-hooks/exhaustive-deps

✖ 6 problems (5 errors, 1 warning)
```

Exit code **1**.

#### TODO / FIXME / HACK

**None** in `app/`. Closest: dashboard “Excel export … is not built yet”; migration “pre-approval … not implemented”; `sampleCollection.ts` comment on unpublished `sampleCollectionQuickAction`.

#### Commented-out abandoned work

No large commented blocks. Abandoned **live** code: `role === "admin"` Settings nav, Settings page, and result-review panel.

#### `any` in security-relevant paths

Only `app/login/page.tsx:16` `catch (err: any)`. Auth error display, not an authorization bypass. Staff/order writes use `Record<string, unknown>` or typed payloads.

`eslint` also flags `<a href>` in nav (client navigation, not auth).

---

### 10. Status table

| Item | Status | Evidence |
| --- | --- | --- |
| Firestore security rules | **Not present** | No `firestore.rules`; no `firebase.json`. |
| Clinic data isolation enforced server-side | **Not present** | Filters only in client `clinicScope.ts`; owner and several queries skip `clinicId`. |
| Owner access to all clinics | **Built & working** | `isOwner` skips `where("clinicId")` (`clinicScope.ts:24–25`); owner console lists all clinics. Client-only. |
| Result approval by `lab_manager` | **Built but broken or unreachable** | Approve UI requires `role === "admin"` (`orders/[orderId]/page.tsx:203,358`). |
| Result approval by `clinic_admin` (should be blocked) | **Built & working** | Same `admin` gate hides the control; not an explicit `clinic_admin` deny. No server deny. |
| Lab manager dashboard / metrics | **Built & working** | `/dashboard` + `canViewDashboard` includes `lab_manager` (`permissions.ts:52–54`, `dashboard/page.tsx`). |
| `sampleCollectedAt` capture | **Built & working** | Written on orders from detail page and patient checkbox; shown in lists/print/TAT. |
| Turnaround time calculation | **Built & working** | Median hours `sampleCollectedAt` → `reviewedAt` for `status==="approved"` (`dashboard/page.tsx:105–128`). Stays empty until anything can be approved. |
| Excel export | **Not present** | Dashboard says so; xlsx is import-only (uncommitted). |
| Email delivery of reports | **Not present** | No mail SDK; only optional support mailto. |
| Storekeeper / inventory module | **Built & working** | Untracked `/inventory*` + `app/lib/inventory.ts`; storekeeper landing `/inventory`. Not in HEAD. Owner specimen write blocked. |
| Bulk spreadsheet import | **Partially built** | Untracked Migration Center imports patients, catalogue, historical orders. Inventory and staff import explicitly unavailable. Not in HEAD. |
| Staff pre-approvals | **Not present** | No collection; migration category marked unavailable. |
| Staff page grouped by clinic | **Built & working** | Working-tree `groups` in `app/staff/page.tsx:297–310`. Uncommitted rewrite. |
| Clinic profile editing by `clinic_admin` | **Not present** | Create-only on `/owner`; no edit UI. |
| Soft delete / recycle bin | **Not present** | `deleteDoc` patients (`patients/page.tsx:194`). |
| Audit log | **Partially built** | Emails/stamps on orders, users, inventory actors; no append-only log collection; no `updatedBy`. |
| Print output | **Built & working** | `/patients/[patientId]/print` + `window.print`. |
| Patient registration | **Built & working** | `/register` with validation, duplicates, consent, Lab ID. Owner blocked. No role matrix enforcement. |
| Test catalogue with reference ranges | **Partially built** | 16 hardcoded tests used as fallback; Settings persist path is `admin`-gated and dead; import can write `testCatalog`. |
| Test ordering | **Built & working** | `/orders/new/[patientId]`. Owner blocked. No role matrix enforcement. |
| Results entry | **Built & working** | Order detail inputs + submit. No role matrix enforcement. |
| Join code flow | **Partially built** | E2E exists; join writes only `clinicId`, not `clinicRoles`. |
| Staff approval + role assignment | **Built & working** | `/staff` pending Approve/Reject + role select; owner can add extra clinic memberships. Working tree is a large uncommitted rewrite. |

---

### 11. Things I did not expect

- Result release is wired to a **removed** role `"admin"` while comments in `permissions.ts` say admin was split into `clinic_admin` / `lab_manager`. The product cannot release a result from the UI. TAT and print-of-results depend on that.
- Settings / catalogue editing is the same dead `"admin"` gate. Clinics that never imported `testCatalog` use the in-memory 16-test list and never persist prices/ranges.
- Owner is both “see everything” and “create nothing” for patients/orders — by design in copy, easy to miss operationally.
- `clinic_admin` **can** register patients, order tests, and enter results (no guards). PRD 3.3 says they should not. Storekeeper can do the same by URL.
- Patient **Delete** is hard-delete, any user on `/patients`, no role check, no cascade to orders.
- Homepage `/` is public and not behind `ProtectedRoute`; `/settings` is also not wrapped (it self-redirects).
- Join is a one-field write. Membership map (`clinicRoles`) is a working-tree concept layered on later.
- Inventory, usernames, profile, migration center, and the staff/auth rewrite are **on disk but not in HEAD**. A clone of HEAD has dashboard + sample collection + multi-tenancy, **no store, no xlsx, no `/profile`**.
- `assignExistingRecords` still claims **every** unscoped/`default-clinic` patient/order/test in the **project**, not “this clinic’s leftovers,” then confirms the signed-in user as owner.
- Layout metadata is still the Create Next App placeholder.
- `firestore.indexes.json` exists without `firebase.json`, so deploying indexes from this repo is not wired.
- No tests, no API routes, no offline persistence.
- `brandColor: null` is written on every new clinic and never read.
- Specimen register is a separate custody log that **does not** set `orders.sampleCollectedAt` (stated on the page).
- Lint currently fails (5 errors); `tsc` is clean.

**HEAD vs disk (precise):** HEAD latest commits cover printing, sample-collection checkbox constraints, dashboard, multi-tenancy, auth, orders, catalogue, patients. Working tree adds memberships/usernames/inventory/migration-center and restyles staff/nav/owner. Auditing “what the app does if you run `next dev` in this folder” includes the uncommitted tree. Auditing GitHub `main` at `acecb8f` does not.
