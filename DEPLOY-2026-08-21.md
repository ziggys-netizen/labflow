# LabFlow Q3R go-live — 21–22 August 2026

**Operator:** Cursor agent (Windows PowerShell)  
**Repo:** `C:\Users\kanui\Documents\LF1` (`ziggys-netizen/labflow`)  
**Production site:** https://labflow-six.vercel.app  
**Firebase project:** `labflow-6cb9e`  
**WIP branch (do not delete):** `wip/inventory-and-migration`  
**Constraint:** no real patient names in the database until Stage C is green.

---

## Rollback target (captured BEFORE any merge, deploy, or rules change)

Recorded 22 August 2026 ~06:48 UTC+4, before touching the tree beyond this file.

| Field | Value |
|---|---|
| Production URL | https://labflow-six.vercel.app |
| Production git SHA (`origin/main`) | `acecb8f8c31424735cc2266c7dd1f9e4df272270` |
| Production commit | *Stop bulk-stamping sample collection from the patient checkbox* (2026-08-21 02:16:42 +0400) |
| Vercel deployment URL (rollback target) | https://labflow-jga2mc03q-buck-holdings.vercel.app |
| Vercel deployment id | `dpl_9WPT2SW5ms25vWi848t7w9okoZgR` |
| Vercel team / project | `buck-holdings` / `labflow` |
| Vercel created | Fri Aug 21 2026 02:17:46 GMT+0400 (matches `acecb8f` push window) |
| Live HTTP | `200` |
| Live `<title>` | `Create Next App` (old production; metadata not yet LabFlow) |
| `X-Vercel-Id` | `lhr1::2hmst-1787366933011-cdade07cf850` |
| `X-Vercel-Cache` | `HIT` |
| `X-Matched-Path` | `/` |
| `X-Nextjs-Prerender` | `1` |
| Region hint | `lhr1` |
| How to roll back | Vercel → Deployments → this known-good production deployment → ⋯ → **Promote to Production**. Do not debug production in place. |
| `gh` | Not installed on this machine |
| Vercel CLI | Authenticated as `ziggys-netizen` in team `buck-holdings` (CLI 59.3.0) |

**Rollback git SHA to promote against:** `acecb8f8c31424735cc2266c7dd1f9e4df272270`  
**Rollback Vercel URL to promote:** https://labflow-jga2mc03q-buck-holdings.vercel.app (`dpl_9WPT2SW5ms25vWi848t7w9okoZgR`)

WIP HEAD at start: `88def442ce5f828cfd77dd57b1b016aadbc53dd8`  
Commits on WIP not in `main`: `7d33de1`, `fbd7375`, `414c903`, `88def44`

---

## Stage A — Merge and deploy

### A1. Pre-flight

**WIP HEAD at start of A1:** `88def442ce5f828cfd77dd57b1b016aadbc53dd8`  
**Fix applied before re-run:** `app/layout.tsx` metadata `Create Next App` → `LabFlow` (tab title / link preview).

| Command | Result | Notes |
|---|---|---|
| `npx tsc --noEmit` | **pass** exit 0 | ~55s; npm `devdir` warning only |
| `npm run lint` | **pass** exit 0 | eslint clean; ~140s |
| `npm test` | **pass** exit 0 | 10 files, 107 tests, ~8.6s |
| `npm run build` | **pass** exit 0 | `vitest run && next build`; compiled in 48s; 26/26 static pages; `/api/health` is `ƒ` |

Did **not** hit the feared A1 failures (`server-only` in a client, missing `"use client"`, prerender Firestore at module scope, missing route export). Tree was already build-clean from Q2R Phase 0.

`next build` warning: Next.js ignored `package-lock.json` in `C:\Users\kanui` because it is outside the git repo. Not a blocker.

Build route table includes `/api/health`, `/api/join/*`, `/api/reports/export`, `/api/cron/pre-approvals/lapse`, `/api/auth/claims/sync`, `/api/staff/pre-approvals*`.

### A2. Merge

```
git checkout main
git pull origin main          # already up to date with origin/main @ acecb8f
git merge wip/inventory-and-migration
```

**Conflicts:** none. Fast-forward `acecb8f` → `1805e84`. WIP is a linear descendant of `main`; no conflict resolution required.

**WIP branch:** still present (`wip/inventory-and-migration` at `1805e84`). Not deleted.

**Merged HEAD:** `1805e8473c57e603b1d89f6db2d3e765afdbd156` — *Set the browser tab title to LabFlow and start the Q3R go-live log.*

`npm run build` on merged `main`: **pass** exit 0 (107 tests; compiled; 26/26 static pages). Same result as A1.

**Pushed:** `git push origin main` — `acecb8f..36e39ea` (2026-08-22).  
**Production commit SHA (this go-live):** `36e39eae0931181754f1fc9b0ae8273538468568`

GitHub↔Vercel auto-deploy started immediately. Did **not** run `npx vercel --prod`.

**Production build in progress:** https://labflow-bq3q7rsi0-buck-holdings.vercel.app (status Building at first poll, ~26s after push).

**WIP branch:** still on remote at `1805e84` (app identical to `36e39ea` except this running log). Not deleted.

### A3. Production environment

`npx vercel env ls` (team `buck-holdings` / project `labflow`), 22 August 2026.

Every `NEXT_PUBLIC_FIREBASE_*` read by `app/lib/firebase.ts`:

| Variable | Production | Preview | Gate |
|---|---|---|---|
| `NEXT_PUBLIC_FIREBASE_API_KEY` | **set** (14d, Sensitive) | set (2h) | checked |
| `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN` | **set** (14d, Sensitive) | set (2h) | checked |
| `NEXT_PUBLIC_FIREBASE_PROJECT_ID` | **set** (14d, Sensitive) | set (2h) | checked |
| `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET` | **set** (14d, Sensitive) | set (2h) | checked |
| `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID` | **set** (14d, Sensitive) | set (2h) | checked |
| `NEXT_PUBLIC_FIREBASE_APP_ID` | **set** (14d, Sensitive) | set (2h) | checked |

No Production Firebase public vars are missing. They were already present; no env add and no extra redeploy required for A3.

**Not set on Production** (expected; later stages): `GOOGLE_CLOUD_PROJECT` / `GCLOUD_PROJECT` / `GCP_*`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY`, `FIREBASE_SERVICE_ACCOUNT_JSON`, `RESEND_API_KEY`, `RESEND_FROM`, `CRON_SECRET`.

**Firebase authorised domains:** `gcloud` and `firebase` CLIs are not installed. Could not confirm `labflow-six.vercel.app` is listed. Human: Firebase Console → Authentication → Settings → Authorised domains. Q3P already noted CLI cannot write Auth settings. Production hostname is the long-standing alias (14d+), so it is likely already authorised; Preview unique hosts may not be.

### A4. Smoke test

New production serving `36e39ea` at https://labflow-six.vercel.app  
Unique deployment: https://labflow-bq3q7rsi0-buck-holdings.vercel.app (`dpl_2kSsknghCf2mAsYpk48voEc1LXRd`) — **Ready**, region `fra1`.  
Unique URL is behind Vercel Deployment Protection (`Login - Vercel`); public alias is open.

| # | Test | Result |
|---|---|---|
| A-1 | Homepage | **pass** — HTTP 200, `<title>LabFlow</title>`, h1 “Laboratory management, built for quality”, JS chunks 200 |
| A-2 | Google sign-in | **blocked: needs human browser** — cannot complete Google popup from this agent |
| A-3 | Role home | **blocked: needs human browser** |
| A-4 | `/patients` | HTML **200** / title LabFlow (prerendered shell). Row population **blocked: needs signed-in browser** |
| A-5 | Register a patient | HTML **200**. Save/Lab ID **blocked: needs signed-in browser**. No test patients written. |
| A-6 | Order → collect → results | **blocked: needs human browser** |
| A-7 | Approve as lab manager | **blocked: needs human browser** |
| A-8 | Awaiting release queue | HTML `/review` **200**. Badge **blocked: needs human browser** |
| A-9 | Print approved result | **blocked: needs human browser** |
| A-10 | Dashboard | HTML **200**. Metrics **blocked: needs human browser** |
| A-11 | Offline `/patients` | **blocked: needs human browser** (devtools) |
| A-12 | Offline approve | **blocked: needs human browser** |
| A-13 | Console uncaught errors | **blocked: needs human browser** |

Also fetched (all **200**, title LabFlow): `/login`, `/register`, `/orders`, `/dashboard`, `/review`, `/inventory`, `/join`, `/settings`, `/profile`, `/staff`, `/owner`, `/pending`. Login copy is client-rendered after auth bootstrap, so prerendered HTML has no “Sign in with Google” string — expected.

**`/api/*` on this first production build:** HTTP **500**, empty body. **Understood — not missing OIDC yet.** Vercel logs:

```
Error: Failed to load external module firebase-admin-…/auth:
Error [ERR_REQUIRE_ESM]: require() of ES Module …/jose/dist/webapi/index.js
from …/jwks-rsa/src/utils.js not supported.
```

`firebase-admin@14` → `jwks-rsa@4` `require()`s `jose@6` (ESM-only) in the Vercel CJS function. Same crash on `/api/health`, join, claims, export, staff, cron.

**Hotfix (this commit):** npm `overrides` pin `jwks-rsa`’s `jose` to `5.10.0` (CJS+ESM). `next.config.ts` sets `serverExternalPackages: ["firebase-admin", "jose", "jwks-rsa"]` so the pin is what the function loads. Local tsc/lint/test/build still pass after the change.

Site UI is **not** broken — **no rollback**. Diagnose on the branch; do not debug production in place.

A4 gate: A-1 pass; A-2–A-13 recorded as blocked (human browser); `/api/*` 500 recorded and patched. Proceeding to redeploy, then Stage B.

**After jose pin (`8351a62`) redeploy:** https://labflow-jsm8skp2h-buck-holdings.vercel.app (`dpl_Edx3XAkHyDdfFZ3j1BVXqkjuGPXh`) — **Ready**, aliased to https://labflow-six.vercel.app.

| Check | Result |
|---|---|
| Homepage title | still **LabFlow** |
| `GET /api/health` | **503** `{"ok":false}` — ESM crash gone; Admin has no credentials (expected until WIF) |
| `GET /api/staff/pre-approvals` (no `Authorization`) | **401** `{"error":"Sign in required."}` |

---

## Stage B — OIDC and server tier (resumed 22 Aug 07:33 UTC+4)

### Four collected values

| Value | Result | How |
|---|---|---|
| GCP project ID | `labflow-6cb9e` | given. `gcloud projects describe` not run — **no gcloud login** |
| GCP project number | `197618102115` (candidate) | public client bundle `messagingSenderId` on labflow-six. Confirm with `gcloud projects describe labflow-6cb9e --format="value(projectNumber)"` after login |
| Vercel team slug | `buck-holdings` | `npx vercel project ls` / inspect / production `oidcTokenClaims.iss` |
| Vercel project name | **`labflow`** | `npx vercel project ls`. Production `sub` is already `owner:buck-holdings:project:labflow:environment:production` |

### B1. Enable OIDC on the Vercel project

**Already existed.** `GET /v9/projects/labflow` → `oidcTokenConfig: { enabled: true, issuerMode: "team" }`.

Production deployment `oidcTokenClaims`:
- `iss`: `https://oidc.vercel.com/buck-holdings`
- `sub`: `owner:buck-holdings:project:labflow:environment:production`
- `aud`: `https://vercel.com/buck-holdings`

No PATCH required. Did not invent a custom issuer.

### B2–B5. WIF pool, provider, SA, principal — **blocked**

Portable Cloud SDK 581.0.0 is on disk:
`%LOCALAPPDATA%\google-cloud-cli-complete\google-cloud-sdk` (bundled Python).

`gcloud auth list` → **No credentialed accounts.**  
`firebase login:list` → **No authorized accounts.**  
No ADC, no `GOOGLE_APPLICATION_CREDENTIALS`, no `CLOUDSDK_AUTH_ACCESS_TOKEN`.

`gcloud auth login --no-launch-browser` printed a Google OAuth URL then **crashed with EOFError** (no TTY to paste the verification code). Cannot complete login from this agent.

**Roles granted:** none.  
**Principal not bound.** Intended string after login (using candidate number + exact project name `labflow`):

```
principal://iam.googleapis.com/projects/197618102115/locations/global/workloadIdentityPools/vercel/subject/owner:buck-holdings:project:labflow:environment:production
```

**Intended `GCP_AUDIENCE` after provider create** (copy from provider details; do not retype if GCP shows a different string):

```
https://iam.googleapis.com/projects/197618102115/locations/global/workloadIdentityPools/vercel/providers/vercel
```

### B6. Vercel Production env

**Not set.** Cannot write `GCP_PROJECT_NUMBER` / `GCP_AUDIENCE` until gcloud confirms the number and the provider exists. Did not invent those values into Vercel.

### B7. Redeploy

**Not done** — no new env vars to apply.

### B8. Health gate

Re-checked during this resume is not required until B7. Last fetch: **503** `{"ok":false}`. Unauth `/api/staff/pre-approvals` was **401**. **STOP before Stage C.**

### B9. Firebase authorised domain

**Not confirmed.** `firebase` 15.28.1 is installed globally; not logged in. Identity Toolkit admin APIs need a credential.

### Exact remaining human action (one login)

In an interactive PowerShell (browser available):

```powershell
$env:CLOUDSDK_PYTHON = "$env:LOCALAPPDATA\google-cloud-cli-complete\google-cloud-sdk\platform\bundledpython\python.exe"
& "$env:LOCALAPPDATA\google-cloud-cli-complete\google-cloud-sdk\bin\gcloud.cmd" auth login
& "$env:LOCALAPPDATA\google-cloud-cli-complete\google-cloud-sdk\bin\gcloud.cmd" config set project labflow-6cb9e
firebase login
```

Then re-run this Stage B order from step 2 (WIF pool). After `gcloud auth list` shows an account, the rest is CLI and does not need console clicking.

### B1. Google Cloud

`gcloud` is **not installed**. Cannot create the Workload Identity Federation pool, OIDC provider, or service account `labflow-vercel`.

**Roles granted:** none (no IAM writes performed).

**Remaining human steps (exact):**

1. Google Cloud Console → project `labflow-6cb9e` → enable **IAM Credentials API** and **Security Token Service API**.
2. IAM & Admin → Workload Identity Federation → Create pool (id e.g. `vercel`).
3. Add OIDC provider:
   - Issuer: `https://oidc.vercel.com/buck-holdings` (Vercel team slug is `buck-holdings`)
   - Map `google.subject` → `assertion.sub`
   - Audience: **Allowed audiences** `https://vercel.com/buck-holdings` *or* GCP default audience (then set `GCP_AUDIENCE` to that URL)
4. Create service account **`labflow-vercel`**. Grant **only**:
   - Cloud Datastore User (or Firestore equivalent read/write on `labflow-6cb9e`)
   - Firebase Authentication Admin (custom claims)
   - **Not** Project Editor or Owner
5. Bind the pool principal for production only:
   `principal://iam.googleapis.com/projects/[PROJECT_NUMBER]/locations/global/workloadIdentityPools/[POOL_ID]/subject/owner:buck-holdings:project:labflow:environment:production`
6. Vercel → Project → Settings → Security / OIDC: enable **OIDC Federation** (team issuer). Redeploy after binding.

### B2. Vercel Production env (not set — cannot invent pool IDs)

| Variable | Production | Notes |
|---|---|---|
| `GCP_PROJECT_ID` | **missing** | should be `labflow-6cb9e` |
| `GCP_PROJECT_NUMBER` | **missing** | from IAM & Admin → Settings |
| `GCP_SERVICE_ACCOUNT_EMAIL` | **missing** | `labflow-vercel@labflow-6cb9e.iam.gserviceaccount.com` after create |
| `GCP_WORKLOAD_IDENTITY_POOL_ID` | **missing** | e.g. `vercel` |
| `GCP_WORKLOAD_IDENTITY_POOL_PROVIDER_ID` | **missing** | e.g. `vercel` |
| `GCP_AUDIENCE` | **missing** | only if using GCP default audience |
| `GOOGLE_CLOUD_PROJECT` / `GCLOUD_PROJECT` | **missing** | OIDC-SETUP.md also lists these |

Code now reads the `GCP_*` vars (see B note below). After the human sets them: **must redeploy**.

### B3. Gate

| Check | Result |
|---|---|
| `https://labflow-six.vercel.app/api/health` → ok | **fail** — 503 `{"ok":false}` |
| No `Authorization` → 401 | **pass** on `/api/staff/pre-approvals` (`Sign in required.`). `/api/health` is an unauthenticated probe by design (200/503 only). |

**STOP.** Do not deploy Firestore rules.

### Code shipped so WIF can work once the human finishes B1/B2

`applicationDefault()` alone does not exchange `VERCEL_OIDC_TOKEN`. `app/lib/firebaseAdmin.ts` now builds an `ExternalAccountClient` when all `GCP_*` pool/SA vars are set, using `@vercel/oidc` + `google-auth-library`. Until those vars exist, behaviour is unchanged (503).

---

## Stage C — Firestore rules and indexes

**Not started.** Gated on `/api/health` ok. Dry-run was **not** run. Rules were **not** deployed.

`firebase` CLI is not installed (would `npm install -g firebase-tools` only after the health gate).

---

## Stage D — Resend and cron

**Not started.** Gated on Stage C green. No Resend account/keys invented. `RESEND_API_KEY`, `RESEND_FROM`, `CRON_SECRET` are not on Production (`vercel env ls`). `vercel.json` already schedules `0 0 * * *` → `/api/cron/pre-approvals/lapse`.

---

## After stages

1. **Q2R** still needs walking against production (phases 2–5, two-clinic isolation). Merging early moved verification later; it did not remove it.
2. **Stage C is not green.** Do not write PRD v0.4 yet. Do not put real patient names in the database.
3. **WIP branch kept:** `wip/inventory-and-migration` (fast-forwarded to match `main` after go-live commits). Not deleted.
4. **No force-push. Hooks were not skipped. Git config was not updated.**

### Production SHAs (this session)

| Role | SHA | Notes |
|---|---|---|
| Rollback target | `acecb8f8c31424735cc2266c7dd1f9e4df272270` | Vercel `dpl_9WPT2SW5ms25vWi848t7w9okoZgR` / https://labflow-jga2mc03q-buck-holdings.vercel.app |
| First Q3R production | `36e39eae0931181754f1fc9b0ae8273538468568` | ESM crash on `/api/*` |
| After jose pin | `8351a620860c24e23ade666837b41db37616c728` | health 503; currently live until OIDC commit deploys |
| OIDC wiring | `346e2374ee0e6f742957e8dab633f8e598e4edfc` | code only |
| Currently live | `53d6faccf96c394d2b26fb4eee1412deaace6845` | https://labflow-cs73dynfe-buck-holdings.vercel.app (`dpl_HsCU5oyQ3nNm4LxYhVsaUq7spHPb`). Health still **503** after this deploy — OIDC imports did not crash. |

### Human blockers

- **Blocking Stage B:** interactive `gcloud auth login` + `firebase login` (no TTY / no existing ADC)
- Browser smoke A-2–A-13 (Google popup, role home, patients, approve, offline, console)
- After login: WIF `vercel` pool/provider, SA `labflow-vercel`, principal bind, Production `GCP_*`, redeploy, health ok
- Then Firestore rules dry-run + deploy (Stage C)
- Resend account, verified domain, `RESEND_*`, `CRON_SECRET` (Stage D)

### Files changed this go-live

- `app/layout.tsx` — tab title LabFlow
- `app/lib/firebaseAdmin.ts` — Vercel OIDC / WIF credential
- `next.config.ts` — `serverExternalPackages`
- `package.json` / `package-lock.json` — jose 5 override; `@vercel/oidc`; `google-auth-library`
- `DEPLOY-2026-08-21.md` — this log
